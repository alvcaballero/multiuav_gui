const filespaht = '/home/arpa/GCS_media/'

export class filesModel {

    static donwload(path) {
      console.log(filespaht+path);
      // check if file exist
      return filespaht+path;
    }
  }
  