// Shared look for 3D-view control buttons, so new controls match the existing
// ones without being forced through a single component.
export const controlSurfaceStyle = {
  backgroundColor: '#fff',
  boxShadow: '0 0 0 2px rgba(0,0,0,0.1)',
  borderRadius: 4,
};
