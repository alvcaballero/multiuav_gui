name: uavs_ssh
root: ~/
on_project_exit: tmux kill-window

windows:
  - uav_1:
      - d=$(date -u +%m%d%H%M%Y.%S)
      - passh -p grvc1234 ssh -t grvc@10.42.0.99 "sudo date -u ${d}; tmuxinator start M210-real"
  - uav_2:
      - d=$(date -u +%m%d%H%M%Y.%S)
      - passh -p ubuntu ssh -t ubuntu@10.42.0.43 "sudo date -u ${d}; tmuxinator start -p ~programming/M210-real.yml"
  - uav_3:
      - d=$(date -u +%m%d%H%M%Y.%S)
      - passh -p rock ssh -t rock@10.42.0.52 "sudo date -u ${d}; tmuxinator start -p ~/programming/px4-real.yml"
